/auth/login:
  post:
    summary: User login
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../components/schemas/UserLoginRequest.yaml'
    responses:
      '200':
        description: Successful login
