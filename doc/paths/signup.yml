/auth/signup:
  post:
    summary: User signup
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../components/schemas/UserSignupRequest.yaml'
    responses:
      '200':
        description: Successful signup
