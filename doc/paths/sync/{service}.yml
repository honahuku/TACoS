/sync/{service}:
  post:
    summary: Sync data with a service
    parameters:
      - $ref: '../components/parameters/ServiceParam.yaml'
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../components/schemas/SyncServiceRequest.yaml'
    responses:
      '200':
        description: Successful sync
