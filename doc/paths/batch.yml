/shifts/batch:
  post:
    summary: Update multiple shifts
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../components/schemas/BatchShiftRequest.yaml'
    responses:
      '200':
        description: Successful update
